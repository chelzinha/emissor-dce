import test from 'node:test';import assert from'node:assert/strict';import{buildTrackingImportTemplate,buildTrackingSnapshotCsv,trackingClosure}from'../src/tracking-closing.js';test('fechamento só fica pronto quando todos têm estado terminal',()=>{const pending=trackingClosure({posted:100,delivered:90,returned:3,inTransit:4,exception:2,awaitingUpdate:1});assert.equal(pending.terminal,93);assert.equal(pending.pending,7);assert.equal(pending.ready,false);assert.equal(pending.completionRate,93);const ready=trackingClosure({posted:100,delivered:97,returned:3});assert.equal(ready.pending,0);assert.equal(ready.ready,true);assert.equal(ready.completionRate,100);assert.equal(ready.deliveryRate,97);});test('relatório CSV preserva campos e escapa ponto e vírgula',()=>{const csv=buildTrackingSnapshotCsv([{trackingCode:'OY855189152BR',service:'SEDEX',category:'DELIVERED',status:'Objeto entregue ao destinatário',description:'Recebido; sem ocorrência',eventAt:'2026-08-22T17:35:00.000Z',location:'Fortaleza/CE',recipient:{name:'Maria Teste',city:'Fortaleza',uf:'CE',zip:'60000000'},reference:'PED-1',source:'CSV:teste.csv'}]);assert.match(csv,/SRO;SERVICO;SITUACAO/);assert.match(csv,/OY855189152BR;SEDEX;Entregue/);assert.match(csv,/"Recebido; sem ocorrência"/);assert.match(csv,/Maria Teste;Fortaleza;CE;60000000/);});test('modelo de importação usa o cabeçalho reconhecido pelo parser atual',()=>{const csv=buildTrackingImportTemplate('23/08/2026');assert.match(csv,/SRO;STATUS;DATA_EVENTO;HORA;LOCAL/);assert.match(csv,/AA000000001BR;Objeto entregue ao destinatário;23\/08\/2026;14:35;Fortaleza\/CE/);});
