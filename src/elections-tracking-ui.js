import "./elections-tracking-ui.css";
import { dataAction } from "./api.js";
const ROOT=document.querySelector("#elections-app");let trackingActive=false;let from="",to="";
function h(v){return String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));}
function fmt(v){return new Intl.NumberFormat("pt-BR").format(Number(v||0));}function cid(){return document.querySelector("#campaign-select")?.value||"";}function date(v){return String(v||"").slice(0,10)}
function label(t){return({LABEL_GENERATED:"Preparados",LABEL_HANDOFF:"Entregues à operação",POSTING_COMPLETED:"Postados",TRACKING_DELIVERED:"Entregues pelos Correios",DCE_PREPARED:"DC-e preparada",DCE_AUTHORIZED:"DC-e autorizada"})[t]||String(t||"").replaceAll("_"," ")}
function qty(events,type,service){return events.filter(e=>e.type===type).reduce((s,e)=>{const direct=String(e.service||"").toUpperCase(),m=e.metadata||{};if(!service)return s+Number(e.quantity||0);if(direct===service)return s+Number(e.quantity||0);if(!direct&&m[service.toLowerCase()]!=null)return s+Number(m[service.toLowerCase()]||0);return s},0)}
function vals(events,service){const prepared=qty(events,"LABEL_GENERATED",service),handoff=qty(events,"LABEL_HANDOFF",service),posted=qty(events,"POSTING_COMPLETED",service),delivered=qty(events,"TRACKING_DELIVERED",service);return{prepared,handoff,posted,transit:Math.max(0,posted-delivered),delivered}}
function lines(v){const base=Math.max(1,v.prepared);return [["Preparados",v.prepared,""],["Entregues à operação",v.handoff,"handoff"],["Postados",v.posted,"posted"],["Em trânsito",v.transit,"transit"],["Entregues",v.delivered,"delivered"]].map(([l,x,c])=>`<div class="tracking-line ${c}"><span>${l}</span><div class="tracking-bar"><i style="width:${Math.min(100,Math.round(x/base*100))}%"></i></div><b>${fmt(x)}</b></div>`).join("")}
function card(name,v,cls){return `<article class="card tracking-service ${cls}"><div class="tracking-head"><strong>${name}</strong><b>${fmt(v.prepared)}</b></div><div class="tracking-lines">${lines(v)}</div></article>`}
async function render(){const campaignId=cid();const slot=ROOT?.querySelector(".app-main>section");if(!slot||!campaignId)return;slot.innerHTML='<div class="page tracking-page"><div class="empty">Carregando acompanhamento...</div></div>';try{const [events,volumes]=await Promise.all([dataAction("operations.list",{campaignId}),dataAction("volumes.list",{campaignId})]);const dates=events.map(e=>date(e.occurredAt)).filter(Boolean).sort();if(!from)from=dates[0]||new Date().toISOString().slice(0,10);if(!to)to=dates.at(-1)||from;const filtered=events.filter(e=>(!from||date(e.occurredAt)>=from)&&(!to||date(e.occurredAt)<=to));const pac=vals(filtered,"PAC"),sedex=vals(filtered,"SEDEX"),total={prepared:pac.prepared+sedex.prepared,handoff:qty(filtered,"LABEL_HANDOFF"),posted:qty(filtered,"POSTING_COMPLETED"),delivered:qty(filtered,"TRACKING_DELIVERED")};total.transit=Math.max(0,total.posted-total.delivered);slot.innerHTML=`<div class="page tracking-page"><div class="page-head"><div><p class="eyebrow">ACOMPANHAMENTO</p><h1>Pós-produção e postagem</h1><p>Baixas acumuladas por período, PAC, SEDEX, TOTAL e volumes físicos.</p></div></div><section class="card tracking-filters"><label class="field"><span>De</span><input id="track-from" type="date" value="${h(from)}"></label><label class="field"><span>Até</span><input id="track-to" type="date" value="${h(to)}"></label><button id="track-apply" class="secondary">Aplicar período</button></section><section class="tracking-services">${card("PAC",pac,"pac")}${card("SEDEX",sedex,"sedex")}${card("TOTAL",total,"total")}</section><section class="tracking-bottom"><article class="card"><div class="section-title"><div><h2>Linha do tempo</h2><p>Eventos do período selecionado.</p></div></div><div class="tracking-timeline">${filtered.slice().sort((a,b)=>String(b.occurredAt).localeCompare(String(a.occurredAt))).slice(0,30).map(e=>`<div class="tracking-event"><time>${h(String(e.occurredAt||"").replace("T"," ").slice(0,16))}</time><strong>${h(label(e.type))}</strong><b>${fmt(e.quantity)}</b></div>`).join("")||'<div class="empty">Nenhum evento no período.</div>'}</div></article><article class="card"><div class="section-title"><div><h2>Volumes físicos</h2><p>Capacidade máxima: 250 etiquetas por volume.</p></div></div><div class="volume-summary">${volumes.slice(0,40).map(v=>`<div class="volume-row"><span>Volume ${fmt(v.number)}/${fmt(v.totalVolumes)} · ${h(v.service)}</span><strong>${fmt(v.quantity)} etiquetas</strong></div>`).join("")||'<div class="empty">Nenhum volume formado.</div>'}</div></article></section></div>`;slot.querySelector("#track-apply").onclick=()=>{from=slot.querySelector("#track-from").value;to=slot.querySelector("#track-to").value;render()}}catch(error){slot.innerHTML=`<div class="page tracking-page"><div class="notice warn">${h(error.message)}</div></div>`}}
function mount(){const nav=ROOT?.querySelector(".app-nav");if(!nav)return;if(!nav.querySelector('[data-view="tracking"]')){const b=document.createElement("button");b.dataset.view="tracking";b.className="nav-track";b.textContent="Acompanhamento";b.addEventListener("click",e=>{e.stopImmediatePropagation();trackingActive=true;nav.querySelectorAll("button").forEach(x=>x.classList.toggle("active",x===b));render()});nav.appendChild(b)}if(trackingActive){const b=nav.querySelector('[data-view="tracking"]');nav.querySelectorAll("button").forEach(x=>x.classList.toggle("active",x===b));const page=ROOT.querySelector(".tracking-page");if(!page)render()}nav.querySelectorAll('button[data-view]:not([data-view="tracking"])').forEach(b=>{if(b.dataset.trackBound)return;b.dataset.trackBound="1";b.addEventListener("click",()=>{trackingActive=false})})}
const observer=new MutationObserver(()=>queueMicrotask(mount));if(ROOT)observer.observe(ROOT,{childList:true,subtree:true});mount();
