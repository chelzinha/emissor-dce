const ROOT=document.querySelector("#elections-app");
const NAV_LABELS={dashboard:"Dashboard",campaigns:"Operações",bases:"Preparação",portal:"Portal Postal",returns:"Retorno do Portal",production:"Produção",tracking:"Acompanhamento"};
const VIEW_STAGE={dashboard:0,campaigns:0,bases:1,portal:3,returns:4,production:5,tracking:8};
const STAGES=[[1,"Receber base","violet"],[2,"Higienizar","violet"],[3,"Exportar Portal","violet"],[4,"Retorno Portal","orange"],[5,"Preparar documentos","orange"],[6,"Impressão","orange"],[7,"Volumes","orange"],[8,"Entrega à operação","green"],[9,"Postagem","green"],[10,"Em trânsito","green"],[11,"Entregues","green"]];
function numberFromText(text){const digits=String(text||"").replace(/\D/g,"");return Number(digits||0)}function formatNumber(v){return new Intl.NumberFormat("pt-BR").format(Number(v||0))}function activeView(){return document.querySelector(".app-nav button.active")?.dataset.view||"dashboard"}
function baseProgress(){const rows=[...(ROOT?.querySelectorAll("tbody tr")||[])].filter(row=>row.querySelector("[data-clean]"));if(!rows.length)return{stage:1,label:"Importe uma base para continuar",action:"",target:"",disabled:true};let firstPending=null,firstReview=null,firstReady=null;for(const row of rows){const cells=row.querySelectorAll("td"),total=numberFromText(cells[2]?.textContent),ready=numberFromText(cells[3]?.textContent),review=numberFromText(cells[4]?.textContent),target=row.querySelector("[data-clean]")?.dataset.clean||"";if(!firstPending&&total>ready+review)firstPending={row,target};if(!firstReview&&review>0)firstReview={row,target};if(!firstReady&&ready>0)firstReady={row,target}}if(firstPending)return{stage:2,label:"Higienizar próximo bloco",action:"clean",target:firstPending.target,disabled:false};if(firstReview)return{stage:2,label:"Revisar pendências antes de exportar",action:"review",target:firstReview.target,disabled:true};if(firstReady)return{stage:3,label:"Exportar CSV PAC/SEDEX",action:"export",target:firstReady.target,disabled:false};return{stage:2,label:"Higienizar base",action:"",target:"",disabled:true}}
function stageForView(view){return view==="bases"?baseProgress().stage:(VIEW_STAGE[view]||0)}
function decorateAuth(){const auth=ROOT?.querySelector(".elections-auth");if(!auth)return;const pill=auth.querySelector(".brand-pill"),title=auth.querySelector(".elections-auth-copy h1"),copy=auth.querySelector(".elections-auth-copy p");if(pill)pill.textContent="AGF OPERAÇÕES POSTAIS";if(title)title.textContent="Da base de endereços à produção postal.";if(copy)copy.textContent="Bases, Portal Postal, documentos, etiquetas, volumes e acompanhamento operacional em um único fluxo."}
function decorateBrandAndNav(){const brand=ROOT?.querySelector(".app-brand");if(brand){const strong=brand.querySelector("strong"),small=brand.querySelector("small");if(strong)strong.textContent="Operações Postais";if(small)small.textContent="Painel da agência"}ROOT?.querySelectorAll(".app-nav button[data-view]").forEach(button=>{const view=button.dataset.view,label=NAV_LABELS[view];if(label&&view!=="tracking")button.textContent=label;button.classList.toggle("nav-prep",["bases","portal"].includes(view));button.classList.toggle("nav-prod",["returns","production"].includes(view));button.classList.toggle("nav-track",view==="tracking")})}
function processMarkup(view){const active=stageForView(view);return `<section class="approved-process" aria-label="Etapas da operação"><div class="approved-process-head"><div><strong>PASSO A PASSO DA OPERAÇÃO</strong><span>O volume avança por etapas e as baixas podem ser parciais.</span></div><div class="approved-process-legend"><i class="done"></i> concluído <i class="current"></i> atual <i class="pending"></i> pendente</div></div><div class="approved-steps">${STAGES.map(([number,label,color])=>{const state=active===0?"neutral":number<active?"done":number===active?"current":"pending";return `<div class="approved-step ${state} ${color}"><b>${number}</b><span>${label}</span></div>`}).join("")}</div></section>`}
function addProcess(){const page=ROOT?.querySelector(".page"),head=page?.querySelector(":scope > .page-head");if(!page||!head||page.querySelector(":scope > .approved-process"))return;const view=activeView();if(view==="campaigns")return;head.insertAdjacentHTML("afterend",processMarkup(view))}
function metricValueByLabel(label){const card=[...(ROOT?.querySelectorAll(".metric-card")||[])].find(item=>item.querySelector("span")?.textContent.trim()===label);return numberFromText(card?.querySelector("strong")?.textContent)}
function addServiceSummary(){const page=ROOT?.querySelector(".page"),metrics=page?.querySelector(".grid.metrics");if(!page||!metrics||page.querySelector(".service-summary")||activeView()!=="dashboard")return;const pac=metricValueByLabel("PAC emitidos"),sedex=metricValueByLabel("SEDEX emitidos"),total=pac+sedex;metrics.insertAdjacentHTML("beforebegin",`<section class="service-summary"><article class="service-card pac"><div><span>PAC</span><small>Objetos emitidos</small></div><strong>${formatNumber(pac)}</strong></article><article class="service-card sedex"><div><span>SEDEX</span><small>Objetos emitidos</small></div><strong>${formatNumber(sedex)}</strong></article><article class="service-card total"><div><span>TOTAL</span><small>PAC + SEDEX emitidos</small></div><strong>${formatNumber(total)}</strong></article></section>`)}
function genericOperationLanguage(){if(activeView()!=="campaigns")return;const head=ROOT?.querySelector(".page-head"),title=head?.querySelector("h1"),description=head?.querySelector("p:not(.eyebrow)"),action=head?.querySelector("#new-campaign");if(title)title.textContent="Operações";if(description)description.textContent="Cada operação mantém seus dados, usuários, lotes e histórico separados.";if(action)action.textContent="Nova operação";ROOT?.querySelectorAll("[data-open-campaign]").forEach(b=>b.textContent="Abrir operação")}
const NEXT_VIEW={portal:["returns","Seguir para Retorno do Portal"],returns:["production","Seguir para Produção"],production:["tracking","Seguir para Acompanhamento"]};
function addNextAction(){const page=ROOT?.querySelector(".page");if(!page||page.querySelector(".approved-next"))return;const view=activeView();if(view==="bases"){const next=baseProgress();page.insertAdjacentHTML("beforeend",`<div class="approved-next"><div><strong>Próximo passo</strong><span>Receba a base, higienize os cadastros e só depois exporte o CSV para o Portal Postal.</span></div><button class="primary" type="button" data-approved-base="${next.action}" data-approved-target="${next.target}" ${next.disabled?"disabled":""}>${next.label}${next.disabled?"":" →"}</button></div>`);return}const next=NEXT_VIEW[view];if(!next)return;page.insertAdjacentHTML("beforeend",`<div class="approved-next"><div><strong>Etapa concluída?</strong><span>Avance mantendo o mesmo contexto da operação selecionada.</span></div><button class="primary" type="button" data-approved-next="${next[0]}">${next[1]} →</button></div>`)}
function keepChipsResponsive(){ROOT?.querySelectorAll(".status").forEach(chip=>chip.title=chip.textContent.trim())}function decorate(){decorateAuth();decorateBrandAndNav();genericOperationLanguage();addProcess();addServiceSummary();addNextAction();keepChipsResponsive()}
ROOT?.addEventListener("click",event=>{const base=event.target.closest("[data-approved-base]");if(base){const action=base.dataset.approvedBase,target=base.dataset.approvedTarget;if(action==="clean"){ROOT.querySelector(`[data-clean="${CSS.escape(target)}"]`)?.click();return}if(action==="export"){const exportButton=ROOT.querySelector(`[data-export="${CSS.escape(target)}"]`);exportButton?.scrollIntoView({behavior:"smooth",block:"center"});exportButton?.focus();return}}const b=event.target.closest("[data-approved-next]");if(!b)return;ROOT.querySelector(`.app-nav button[data-view="${CSS.escape(b.dataset.approvedNext)}"]`)?.click()});let scheduled=false;const observer=new MutationObserver(()=>{if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;decorate()})});if(ROOT)observer.observe(ROOT,{childList:true,subtree:true});decorate();
