import './homologacao-demo.css';
import { DEMO_CAMPAIGN,DEMO_TRACKING,DEMO_TRACKING_EVENTS,createDemoState,financeSummary } from './homologacao-data.js';

const root=document.querySelector('#portal-homologacao-app');
const state=createDemoState();
let view='dashboard';
const h=value=>String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
const num=value=>new Intl.NumberFormat('pt-BR').format(Number(value||0));
const money=value=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(value||0));
const dateLabel=value=>String(value||'').split('-').reverse().join('/');
const NAV=[['dashboard','Dashboard'],['simulador','Simulador'],['autorizacao','Autorizar DC-e']];

function toast(message){let box=document.querySelector('.demo-toast');if(!box){box=document.createElement('div');box.className='demo-toast';document.body.appendChild(box)}box.textContent=message;box.classList.add('show');clearTimeout(box._timer);box._timer=setTimeout(()=>box.classList.remove('show'),3200)}
function bar(label,value,total,cls=''){const width=total?Math.min(100,Math.round(value/total*100)):0;return `<div class="bar-row ${cls}"><span>${label}</span><div class="bar-track"><i style="width:${width}%"></i></div><b>${num(value)}</b></div>`}
function service(name,data){return `<article class="card service-card"><div class="service-title"><strong>${name}</strong><b>${num(data.posted)}</b></div>${bar('Em trânsito',data.inTransit,data.posted)}${bar('Saiu para entrega',data.outForDelivery,data.posted)}${bar('Entregues',data.delivered,data.posted,'delivered')}${bar('Ocorrências',data.exception,data.posted,'warning')}</article>`}
function dailyTable(){return `<div class="table-wrap"><table class="demo-table"><thead><tr><th>Data</th><th>PAC</th><th>SEDEX</th><th>Objetos</th><th>Valor postado</th><th>Pago</th><th>Saldo</th></tr></thead><tbody>${state.daily.map(row=>`<tr><td>${dateLabel(row.date)}</td><td>${num(row.pacQuantity)}</td><td>${num(row.sedexQuantity)}</td><td>${num(row.totalQuantity)}</td><td>${money(row.totalAmount)}</td><td>${money(row.paid)}</td><td>${money(row.balance)}</td></tr>`).join('')}</tbody></table></div>`}

function dashboard(){const f=financeSummary(state),t=DEMO_TRACKING.total;return `<div class="portal-hero"><h1>Acompanhamento da operação</h1><p>Postagens, entrega, valores e pagamentos em um único painel.</p></div><div class="demo-banner"><div><b>Visualização demonstrativa</b><small>Os números são fictícios e servem apenas para avaliar o portal.</small></div><span class="status warn">HOMOLOGAÇÃO</span></div><section class="portal-finance"><article><span>Valor postado</span><b>${money(f.totalAmount)}</b></article><article><span>Valor pago</span><b>${money(f.received)}</b></article><article><span>Saldo em aberto</span><b>${money(f.outstanding)}</b></article><article><span>Objetos entregues</span><b>${num(t.delivered)} de ${num(t.posted)}</b></article></section><section class="grid-three">${service('PAC',DEMO_TRACKING.pac)}${service('SEDEX',DEMO_TRACKING.sedex)}${service('TOTAL',DEMO_TRACKING.total)}</section><section class="card section"><div class="section-head"><div><h2>Postagens e valores por dia</h2><p>Consolidado financeiro disponibilizado pela agência.</p></div></div>${dailyTable()}</section><section class="card section"><div class="section-head"><div><h2>Últimas atualizações</h2><p>Histórico resumido dos objetos da campanha.</p></div></div><div class="timeline">${DEMO_TRACKING_EVENTS.slice(0,5).map(item=>`<div class="timeline-row"><time>${h(new Date(item.eventAt).toLocaleString('pt-BR'))}</time><code>${h(item.trackingCode)}</code><span>${h(item.status)}<br><small class="muted">${h(item.location)}</small></span><span class="status ${item.category==='DELIVERED'?'ok':item.category==='EXCEPTION'||item.category==='RETURNING'?'warn':''}">${h(item.category.replaceAll('_',' '))}</span></div>`).join('')}</div></section>`}

function simulador(){return `<div class="portal-hero"><h1>Simulador</h1><p>Estimativa demonstrativa de quantidade e distribuição postal.</p></div><section class="grid-two"><article class="card section"><div class="section-head"><div><h2>Nova simulação</h2><p>Nenhuma tarifa real é usada nesta homologação.</p></div></div><form id="sim-form" class="finance-form"><label class="field"><span>CEP de destino</span><input name="cep" maxlength="8" placeholder="60000000" required></label><label class="field"><span>Quantidade</span><input name="quantity" type="number" min="1" value="100" required></label><label class="field"><span>Serviço</span><select name="service"><option>PAC</option><option>SEDEX</option></select></label><label class="field"><span>Peso médio</span><select name="weight"><option>Até 300 g</option><option>Até 500 g</option><option>Até 1 kg</option></select></label><button class="primary wide">Simular distribuição</button></form></article><article class="card section"><div class="section-head"><div><h2>Resultado</h2><p>Exemplo sem preços comerciais.</p></div></div><div id="sim-result" class="empty">Preencha os dados para visualizar uma estimativa operacional.</div></article></section>`}

function autorizacao(){return `<div class="portal-hero"><h1>Autorizar DC-e</h1><p>Lotes preparados pela agência para autorização com o e-CNPJ da campanha.</p></div><section class="grid-two"><article class="card section"><div class="section-head"><div><h2>Lote DC-e #DEMO-184</h2><p>Preparado em 25/08/2026 às 17:20</p></div><span class="status warn">Aguardando autorização</span></div><div class="report-summary"><div><span>Documentos</span><b>622</b></div><div><span>PAC</span><b>622</b></div><div><span>SEDEX</span><b>0</b></div><div><span>Ambiente</span><b>Homologação</b></div></div><button class="primary" id="demo-authorize" style="margin-top:15px">Visualizar etapa de autorização</button></article><article class="card section"><div class="section-head"><div><h2>e-CNPJ A1</h2><p>O certificado real não deve ser usado nesta página demonstrativa.</p></div></div><label class="field"><span>Certificado</span><input type="file" disabled></label><label class="field" style="margin-top:10px"><span>Senha</span><input type="password" disabled placeholder="Bloqueado na demonstração"></label><div class="demo-banner" style="margin-top:14px;margin-bottom:0"><small>A autorização fiscal continua desativada para proteger o certificado e o ambiente real.</small></div></article></section>`}

function render(){const content=view==='dashboard'?dashboard():view==='simulador'?simulador():autorizacao();root.innerHTML=`<div class="portal-demo"><aside class="portal-side"><img src="/agf-logo.png" alt="AGF José Bonifácio"><nav>${NAV.map(([id,label])=>`<button data-view="${id}" class="${view===id?'active':''}">${label}</button>`).join('')}</nav></aside><main class="portal-main"><header class="portal-head"><strong>${NAV.find(item=>item[0]===view)?.[1]}</strong><div><span>${h(DEMO_CAMPAIGN.name)}</span> <a href="/homologacao.html" class="secondary" style="text-decoration:none;margin-left:10px">Painel da AGF</a></div></header><div class="portal-body">${content}</div></main><nav class="mobile-tabs">${NAV.map(([id,label])=>`<button data-view="${id}" class="${view===id?'active':''}">${label}</button>`).join('')}</nav></div>`;bind()}

function bind(){root.querySelectorAll('[data-view]').forEach(button=>button.onclick=()=>{view=button.dataset.view;render()});root.querySelector('#sim-form')?.addEventListener('submit',event=>{event.preventDefault();const values=Object.fromEntries(new FormData(event.currentTarget));root.querySelector('#sim-result').innerHTML=`<strong>${h(values.service)} para ${num(values.quantity)} objetos</strong><br><br>Destino-base: ${h(values.cep)}<br>Peso médio: ${h(values.weight)}<br><br><span class="status ok">Simulação operacional pronta</span><p class="muted">O valor só será exibido quando uma tabela comercial real estiver vinculada à operação.</p>`});root.querySelector('#demo-authorize')?.addEventListener('click',()=>toast('Etapa demonstrativa. Nenhum certificado ou DC-e foi processado.'))}

render();
