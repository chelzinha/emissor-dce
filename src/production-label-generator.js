import { PDFDocument, StandardFonts, rgb } from 'pdf-lib';
import bwipjs from 'bwip-js';
import QRCode from 'qrcode';
import { dataAction, downloadBlob } from './api.js';
import { getPortalReturnAssets } from './portal-assets.js';
import { auditPdfDocuments, loadPdfDocuments, verifyCrops } from './matrix-engine.js';
import { loadPostalVendors } from './postal-vendors.js';

const MM=72/25.4, PAGE_W=100, PAGE_H=150, M=4;
const mm=value=>value*MM;
const INK=rgb(0.02,0.05,0.08), MUTED=rgb(0.30,0.34,0.39), SOFT=rgb(0.90,0.90,0.90);
const CROP_CACHE=new Map();
const Z={top:23,tracking:20,receiver:8.5,recipient:23,sender:12,sep:2.5,title:4.2,ident:9.5,table:13,items:6,legal:19.5};
const OBS_LEGAL='É contribuinte de ICMS qualquer pessoa física ou jurídica, que realize, com habitualidade ou em volume que caracterize intuito comercial, operações de circulação de mercadoria ou prestações de serviços de transportes interestadual e intermunicipal e de comunicação, ainda que as operações e prestações se iniciem no exterior, conforme art. 4º da Lei Complementar nº 87/96. Constitui crime contra a ordem tributária suprimir ou reduzir tributo, ou contribuição social e qualquer acessório: quando negar ou deixar de fornecer, quando obrigatório, nota fiscal ou documento equivalente, relativa a venda de mercadoria ou prestação de serviço, efetivamente realizada ou fornecê-la em desacordo com a legislação, sob pena de reclusão de dois a cinco anos, e multa, conforme inciso V do art. 1º da Lei nº 8.137/90.';

function normalizeTracking(value){return String(value||'').replace(/\s/g,'').toUpperCase();}
function digits(value){return String(value??'').replace(/\D/g,'');}
function upper(value){return String(value??'').toUpperCase();}
function moneyNumber(value){if(typeof value==='number')return Number.isFinite(value)?value:0;let text=String(value??'').trim().replace(/[^0-9,.-]/g,'');if(!text)return 0;const comma=text.lastIndexOf(','),dot=text.lastIndexOf('.');if(comma>=0&&dot>=0){if(comma>dot)text=text.replace(/\./g,'').replace(',','.');else text=text.replace(/,/g,'');}else if(comma>=0)text=text.replace(/\./g,'').replace(',','.');else if((text.match(/\./g)||[]).length>1)text=text.replace(/\.(?=.*\.)/g,'');const number=Number(text);return Number.isFinite(number)?number:0;}
function money(value){return moneyNumber(value).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});}
function fmtDoc(value){const d=digits(value);if(d.length===14)return d.replace(/^(\d{2})(\d{3})(\d{3})(\d{4})(\d{2})$/,'$1.$2.$3/$4-$5');if(d.length===11)return d.replace(/^(\d{3})(\d{3})(\d{3})(\d{2})$/,'$1.$2.$3-$4');return d||'-';}
function fmtCep(value){const d=digits(value);return d.length===8?`${d.slice(0,5)}-${d.slice(5)}`:d;}
function fmtKey(value){return digits(value).match(/.{1,4}/g)?.join(' ')||'';}
function serviceDefaults(service){const s=upper(service);return s.startsWith('SEDEX')?{modal:'EXPRESSA',stripe:'SIM'}:{modal:'STANDARD',stripe:'IMP'};}
function postalValue(postal,...keys){for(const key of keys){if(postal?.[key]!=null&&String(postal[key]).trim()!=='')return postal[key];}return '';}
function declaredValue(object){return moneyNumber(postalValue(object.postal,'VALOR_DECLARADO','VALOR','VLR_DECLARADO','DECLARED_VALUE'));}
function topY(topMm){return mm(PAGE_H-topMm);}
function rect(page,x,top,w,h,options={}){page.drawRectangle({x:mm(x),y:mm(PAGE_H-top-h),width:mm(w),height:mm(h),borderWidth:options.borderWidth??0.45,borderColor:options.borderColor||INK,color:options.fill});}
function line(page,x1,y1,x2,y2,width=.4,dash){page.drawLine({start:{x:mm(x1),y:topY(y1)},end:{x:mm(x2),y:topY(y2)},thickness:width,dashArray:dash?.map(mm),color:INK});}
function fitText(page,font,bold,value,x,top,maxWidth,size=8,minSize=4.2,align='left',color=INK){let text=String(value??'');let current=size;const f=bold;while(current>minSize&&f.widthOfTextAtSize(text,current)>mm(maxWidth))current-=.25;if(f.widthOfTextAtSize(text,current)>mm(maxWidth)){while(text.length>1&&f.widthOfTextAtSize(text+'…',current)>mm(maxWidth))text=text.slice(0,-1);text+='…';}let xx=mm(x);const width=f.widthOfTextAtSize(text,current);if(align==='center')xx=mm(x)+((mm(maxWidth)-width)/2);if(align==='right')xx=mm(x)+mm(maxWidth)-width;page.drawText(text,{x:xx,y:topY(top)-current,color,font:f,size:current});return current;}
function wrapText(font,value,size,maxWidth){const words=String(value||'').split(/\s+/).filter(Boolean),lines=[];let row='';for(const word of words){const next=row?`${row} ${word}`:word;if(font.widthOfTextAtSize(next,size)<=mm(maxWidth))row=next;else{if(row)lines.push(row);row=word;}}if(row)lines.push(row);return lines;}
function blockText(page,font,value,x,top,w,h,maxSize=5.2,minSize=3.5){for(let size=maxSize;size>=minSize;size-=.2){const lines=wrapText(font,value,size,w),lineMm=size*.3528*1.12;if(lines.length*lineMm<=h){lines.forEach((row,index)=>page.drawText(row,{x:mm(x),y:topY(top+index*lineMm)-size,font,size,color:INK}));return;}}fitText(page,font,font,value,x,top,w,minSize,minSize);}
async function barcodeDataUrl(text,options={}){const canvas=document.createElement('canvas');bwipjs.toCanvas(canvas,{bcid:'code128',text:String(text),scale:2,height:options.height||10,includetext:false,padding:0});return canvas.toDataURL('image/png');}
async function matrixCrops(portalReturnId,onProgress){if(CROP_CACHE.has(portalReturnId))return CROP_CACHE.get(portalReturnId);const assets=await getPortalReturnAssets(portalReturnId);if(!assets?.pdfFiles?.length)throw new Error('Os PDFs originais do Portal não estão disponíveis neste navegador. Reimporte o retorno no computador da produção.');const {pdfjsLib,ZXing}=await loadPostalVendors();const documents=await loadPdfDocuments(assets.pdfFiles,pdfjsLib);const audit=await auditPdfDocuments(documents,ZXing,{onProgress});const verified=await verifyCrops(audit.crops,ZXing);const failed=verified.filter(row=>!row.ok);if(failed.length)throw new Error(`${failed.length} Data Matrix falharam na releitura antes da geração.`);CROP_CACHE.set(portalReturnId,audit.crops);return audit.crops;}
async function image(pdf,dataUrl){return pdf.embedPng(dataUrl);}
function recipientAddress(object){const r=object.recipient||{},a=r.address||{};return{document:r.document||r.cpfCnpj||'',name:r.name||'',street:a.street||'',number:a.number||'',complement:a.complement||'',district:a.district||'',city:a.city||'',uf:a.uf||'',zip:a.zip||''};}
function senderAddress(object){const s=object.sender||{},a=s.address||{};return{document:s.document||'',name:s.name||'',street:a.street||'',number:a.number||'',complement:a.complement||'',district:a.district||'',city:a.city||'',uf:a.uf||'',zip:a.zip||''};}
function drawReceiver(page,fonts,top){fitText(page,fonts.regular,fonts.regular,'Recebedor:',M,top+3,18,7,5);line(page,M+17,top+3.1,96,top+3.1,.3);fitText(page,fonts.regular,fonts.regular,'Assinatura:',M,top+7,18,7,5);line(page,M+17,top+7.1,50,top+7.1,.3);fitText(page,fonts.regular,fonts.regular,'Documento:',55,top+7,18,7,5);line(page,75,top+7.1,96,top+7.1,.3);}
async function drawPostal(page,pdf,fonts,object,matrixDataUrl){const r=recipientAddress(object),s=senderAddress(object),service=upper(object.service),defaults=serviceDefaults(service),postal=object.postal||{},stripe=String(object.matrix?.stripe||defaults.stripe),modal=defaults.modal;let y=M;rect(page,M,y,31,18);fitText(page,fonts.bold,fonts.bold,'CHANCELA',M+2,y+4,27,8,5,'center');fitText(page,fonts.bold,fonts.bold,'CLIENTE',M+2,y+10,27,7,5,'center');const xDm=58,xSym=85;fitText(page,fonts.bold,fonts.bold,`PP: ${postalValue(postal,'CODIGO_PP','CARTAO_POSTAGEM','CARTAO')||'—'}`,37,y+4,19,6.5,4.5);fitText(page,fonts.bold,fonts.bold,'À VISTA',37,y+9,19,7,5);fitText(page,fonts.bold,fonts.bold,service,37,y+14,19,7,5);const dm=await image(pdf,matrixDataUrl);page.drawImage(dm,{x:mm(xDm),y:mm(PAGE_H-y-25),width:mm(25),height:mm(25)});rect(page,xSym,y,11,17,{fill:SOFT});fitText(page,fonts.bold,fonts.bold,service,xSym+1,y+7,9,7,4.5,'center');fitText(page,fonts.bold,fonts.bold,modal,xSym-2,y+22,15,5.5,4,'center');y+=Z.top;const tracking=normalizeTracking(object.trackingCode);fitText(page,fonts.bold,fonts.bold,tracking,M+3,y+5,86,13,8,'center');const trackBarcode=await image(pdf,await barcodeDataUrl(tracking,{height:9}));page.drawImage(trackBarcode,{x:mm(10),y:mm(PAGE_H-y-19),width:mm(80),height:mm(11.5)});y+=Z.tracking;drawReceiver(page,fonts,y);y+=Z.receiver;rect(page,M,y,92,Z.recipient);rect(page,M,y,30,4.6,{fill:INK});fitText(page,fonts.bold,fonts.bold,'Destinatário',M+2,y+3.4,26,7,5,'center',rgb(1,1,1));fitText(page,fonts.regular,fonts.regular,`CPF/CNPJ: ${fmtDoc(r.document)}`,36,y+3.4,24,5,4);fitText(page,fonts.bold,fonts.bold,upper(r.name),M+2,y+9,55,10.5,6);fitText(page,fonts.regular,fonts.regular,upper([r.street,r.number].filter(Boolean).join(', ')),M+2,y+13.2,55,8,5);fitText(page,fonts.regular,fonts.regular,upper([r.complement,r.district].filter(Boolean).join(', ')),M+2,y+17,55,7,4.5);fitText(page,fonts.bold,fonts.bold,fmtCep(r.zip),M+2,y+21.2,28,10,6);fitText(page,fonts.regular,fonts.regular,upper(`${r.city} / ${r.uf}`),34,y+21.2,25,8,5);if(digits(r.zip).length===8){const cepBarcode=await image(pdf,await barcodeDataUrl(digits(r.zip),{height:10}));page.drawImage(cepBarcode,{x:mm(62),y:mm(PAGE_H-y-18.5),width:mm(19.7),height:mm(13)});}y+=Z.recipient;rect(page,M,y,92,Z.sender);fitText(page,fonts.bold,fonts.bold,'Remetente:',M+1.5,y+3,18,6.5,4.5);fitText(page,fonts.bold,fonts.bold,upper(s.name),M+1.5,y+6.2,78,7.2,4.5);fitText(page,fonts.regular,fonts.regular,upper([s.street,s.number,s.complement,s.district].filter(Boolean).join(', ')),M+1.5,y+8.9,78,6.4,4);fitText(page,fonts.bold,fonts.bold,upper(`${fmtCep(s.zip)} ${s.city}/${s.uf}`),M+1.5,y+11.2,33,6,4);fitText(page,fonts.regular,fonts.regular,`CNPJ/CPF: ${fmtDoc(s.document)}`,40,y+11.2,40,6,4);rect(page,88,y,8,Z.sender,{fill:INK});fitText(page,fonts.bold,fonts.bold,stripe,88.5,y+6.8,7,6,4,'center',rgb(1,1,1));y+=Z.sender;line(page,M,y+Z.sep/2,96,y+Z.sep/2,.45,[1.6,1.2]);return y+Z.sep;}
function drawPartyTable(page,fonts,object,top,issuer){const r=recipientAddress(object),s=issuer?{document:issuer.cnpj||issuer.document||'',name:issuer.name||'',street:issuer.address?.street||'',number:issuer.address?.number||'',city:issuer.address?.city||'',uf:issuer.address?.uf||''}:senderAddress(object),half=46;rect(page,M,top,92,Z.table);rect(page,M,top,half,3.7,{fill:SOFT});rect(page,M+half,top,half,3.7,{fill:SOFT});fitText(page,fonts.bold,fonts.bold,'REMETENTE',M+1,top+2.8,half-2,6.2,4.5,'center');fitText(page,fonts.bold,fonts.bold,'DESTINATÁRIO',M+half+1,top+2.8,half-2,6.2,4.5,'center');const rows=[['CNPJ/CPF:',fmtDoc(s.document),fmtDoc(r.document)],['NOME:',upper(s.name),upper(r.name)],['CIDADE-UF:',upper(`${s.city||''}-${s.uf||''}`),upper(`${r.city||''}-${r.uf||''}`)],['ENDEREÇO:',upper([s.street,s.number].filter(Boolean).join(', ')),upper([r.street,r.number].filter(Boolean).join(', '))]];rows.forEach((row,i)=>{const yy=top+5.3+i*2.35;fitText(page,fonts.regular,fonts.regular,`${row[0]} ${row[1]}`,M+1.2,yy,half-2.4,5,3.7);fitText(page,fonts.regular,fonts.regular,`${row[0]} ${row[2]}`,M+half+1.2,yy,half-2.4,5,3.7);});}
function drawItems(page,fonts,object,top,items){const value=items?.reduce((sum,item)=>sum+Number(item.totalValue??(Number(item.quantity||1)*Number(item.unitValue||0))),0)??declaredValue(object),description=items?.map(item=>item.description).filter(Boolean).join('; ')||object.content||'-',quantity=items?.reduce((sum,item)=>sum+Number(item.quantity||1),0)||1;rect(page,M,top,92,Z.items);fitText(page,fonts.regular,fonts.regular,'DESCRIÇÃO DOS BENS OU MERCADORIAS',M+1,top+2,55,4.5,3.5);fitText(page,fonts.regular,fonts.regular,'QTD',62,top+2,10,4.5,3.5,'center');fitText(page,fonts.regular,fonts.regular,'VALOR TOTAL',73,top+2,22,4.5,3.5,'right');fitText(page,fonts.bold,fonts.bold,description,M+1,top+5.4,55,7.5,4);fitText(page,fonts.bold,fonts.bold,String(quantity),62,top+5.4,10,7,4,'center');fitText(page,fonts.bold,fonts.bold,money(value),73,top+5.4,22,7,4,'right');}
function drawSimplified(page,fonts,object,top){rect(page,M,top,92,Z.title);fitText(page,fonts.bold,fonts.bold,'DECLARAÇÃO SIMPLIFICADA DE CONTEÚDO',M+1,top+3.1,90,7.2,5,'center');top+=Z.title;rect(page,M,top,92,Z.ident,{fill:rgb(.98,.98,.98)});fitText(page,fonts.bold,fonts.bold,'Documento operacional vinculado a esta postagem.',M+2,top+2.7,88,5.8,4.2,'center');fitText(page,fonts.bold,fonts.bold,'Não representa DC-e autorizada; não possui chave ou protocolo fiscal.',M+2,top+5.6,88,5.2,3.8,'center');fitText(page,fonts.regular,fonts.regular,'Conteúdo declarado pelo remetente para fins operacionais da postagem.',M+2,top+8.3,88,5.2,3.8,'center');top+=Z.ident;drawPartyTable(page,fonts,object,top,null);top+=Z.table;drawItems(page,fonts,object,top,null);top+=Z.items;rect(page,M,top,92,Z.legal);blockText(page,fonts.regular,'Declaro, sob minha responsabilidade, que o conteúdo indicado nesta declaração corresponde ao objeto apresentado para postagem. Este documento possui finalidade operacional e não substitui documento fiscal ou DC-e quando estes forem legalmente exigidos.',M+2,top+2.5,88,Z.legal-3.5,5.2,3.8);}
async function drawDace(page,pdf,fonts,object,top){const d=object.dce||{},issuer=d.issuer||object.sender||{},items=d.items||null;rect(page,M,top,92,Z.title);fitText(page,fonts.bold,fonts.bold,'DACE RESUMIDA - DECLARAÇÃO AUXILIAR DE CONTEÚDO ELETRÔNICA',M+1,top+3.1,90,6.8,4.6,'center');top+=Z.title;rect(page,M,top,92,Z.ident);fitText(page,fonts.regular,fonts.regular,`Nº: ${String(d.number||0).padStart(9,'0')}   SÉRIE: ${String(d.series||0).padStart(3,'0')}`,M+1,top+2.8,45,5.6,4);fitText(page,fonts.regular,fonts.regular,`DATA EMISSÃO: ${String(d.authorizedAt||'').replace('T',' ').slice(0,19)}`,51,top+2.8,44,5.3,3.8);fitText(page,fonts.regular,fonts.regular,`PROTOCOLO AUTORIZAÇÃO: ${d.protocol}`,M+1,top+5.6,55,5.4,3.8);fitText(page,fonts.regular,fonts.regular,'MODALIDADE DE TRANSPORTE: 0 - CORREIOS',M+1,top+8.3,55,5.2,3.8);fitText(page,fonts.bold,fonts.bold,`CHAVE: ${fmtKey(d.accessKey)}`,60,top+8.3,35,4.2,3.4);top+=Z.ident;drawPartyTable(page,fonts,object,top,issuer);top+=Z.table;drawItems(page,fonts,object,top,items);top+=Z.items;rect(page,M,top,92,Z.legal);const qrUrl=`https://www.fazenda.pr.gov.br/dce/qrcode?chDCe=${digits(d.accessKey)}&tpAmb=${d.environment||'2'}`,qrData=await QRCode.toDataURL(qrUrl,{margin:0,width:180,errorCorrectionLevel:'M'}),qr=await image(pdf,qrData);page.drawImage(qr,{x:mm(M+1.5),y:mm(PAGE_H-top-14),width:mm(13),height:mm(13)});blockText(page,fonts.regular,OBS_LEGAL,M+16,top+2.1,78,Z.legal-3,4.8,3.3);}
async function renderLabel(pdf,fonts,object,crop){const page=pdf.addPage([mm(PAGE_W),mm(PAGE_H)]);let top=await drawPostal(page,pdf,fonts,object,crop);if(object.dce)await drawDace(page,pdf,fonts,object,top);else drawSimplified(page,fonts,object,top);return page;}
async function buildPdf(data,onProgress){if(data.senderIssues?.length)throw new Error(`Dados do remetente incompletos: ${data.senderIssues.join(' ')}`);const crops=await matrixCrops(data.portalReturnId,p=>onProgress?.(`Lendo Data Matrix: ${p.processed||0}/${p.totalPages||0}`)),pdf=await PDFDocument.create(),fonts={regular:await pdf.embedFont(StandardFonts.Helvetica),bold:await pdf.embedFont(StandardFonts.HelveticaBold)};for(let index=0;index<data.objects.length;index++){const object=data.objects[index],crop=crops.get(normalizeTracking(object.trackingCode));if(!crop)throw new Error(`Data Matrix original não localizado para ${object.trackingCode}.`);onProgress?.(`Montando etiqueta ${index+1} de ${data.objects.length}`);await renderLabel(pdf,fonts,object,crop);if(index%20===0)await new Promise(resolve=>setTimeout(resolve,0));}pdf.setTitle(data.documentMode==='DCE_AUTHORIZED'?'Etiquetas unificadas DC-e':'Etiquetas com declaração simplificada');pdf.setProducer('AGF Operações Postais');return pdf.save();}
export async function generateProductionTestPdf(campaignId,productionBatchId,onProgress){const data=await dataAction('production.documents.test',{campaignId,productionBatchId}),bytes=await buildPdf(data,onProgress);downloadBlob(new Blob([bytes],{type:'application/pdf'}),`etiqueta_teste_${String(data.objects[0]?.trackingCode||'lote')}.pdf`);return data;}
export async function generateProductionVolumePdf(campaignId,productionBatchId,volumeId,onProgress){const data=await dataAction('production.documents.volume',{campaignId,productionBatchId,volumeId}),bytes=await buildPdf(data,onProgress),v=data.volume;downloadBlob(new Blob([bytes],{type:'application/pdf'}),`volume_${String(v.number).padStart(2,'0')}_de_${String(v.totalVolumes).padStart(2,'0')}_${v.service}.pdf`);return data;}
